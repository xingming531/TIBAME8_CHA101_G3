#MySQL
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url=jdbc:mysql://localhost:3306/gp3
#spring.datasource.username=root
#spring.datasource.password=123


# AWS RDS (moved to sensitives.properties)

#SQL execution command
logging.level.org.hibernate.SQL=debug


# Redis connection config
spring.redis.host=localhost
spring.redis.port=6379
# 10_Xing use database 1
spring.redis.database=1
# Redis connection pool config
spring.redis.jedis.pool.max-active=8
spring.redis.jedis.pool.max-idle=8
spring.redis.jedis.pool.min-idle=0
spring.redis.jedis.pool.max-wait=-1

#SQL execution command
#logging.level.org.hibernate.SQL=debug

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Disable Thymeleaf, we use html/css/js to create frontend.
# spring.thymeleaf.enabled=false
# Disable Whitelabel Error Page, we use our own error page.
server.error.whitelabel.enabled=false
# Datasource Pool Config
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# The two properties below might generate excessive log output. Uncomment them if needed.
# JPA/Hibernate Config
# spring.jpa.show-sql=true
# Beautify jpa console logging
# spring.jpa.properties.hibernate.format_sql=true

# Logging Config, uncomment if needed.
#logging.level.tw.idv.petradisespringboot=debug
#logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%level] [%t] %msg%n
#logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} [%level] [%t] [%line] %msg%n
#logging.file.path=./

# Gmail SMTP (moved to sensitives.properties)



